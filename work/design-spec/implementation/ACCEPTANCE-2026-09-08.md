# 2026-09-08 验收记录

本轮只完善测试，不改已确认 UI、图标或业务代码。所有测试记录写入模拟存储，不接触用户个人数据。

## 新增覆盖

| 场景 | 验证内容 | 结果 |
| --- | --- | --- |
| 月底日期切换 | 2027-01-31 → 02-28；2028-01-31 → 02-29；无二月无效日期 | 通过 |
| 小屏长回忆 | 320×568、1.3 倍字体，长文字可滚动到末尾，无布局溢出；猫狗区域在文字之后 | 通过 |
| 夸夸新增/修改/重新加载 | 实际 Widget 按钮与输入流程，销毁再挂载 App 后读取修改内容；记录 ID、创建时间及历史记录保留 | 通过 |

测试文件：`app/test/recall_test.dart`、`app/test/widget_test.dart`。每项 Widget 测试清理测试资源缓存，避免前项异步缓存影响后一项加载；生产资源加载逻辑未动。

## 完整检查

- `flutter analyze --no-pub`：无问题。
- `flutter test --no-pub`：19 项通过，包括既有记录序列化、每日第一条夸夸与历史排列、收支摘要、中文日期选择器等测试。
- `flutter build apk --debug --no-pub`：成功，输出 `app/build/app/outputs/flutter-apk/app-debug.apk`。页面和功能与上一版一致，不应把本轮称为新增功能版本。
- 构建有既有 speech_to_text Kotlin Gradle 与 SDK XML 版本警告，尚未处理，不影响本次 Debug 构建通过。
- 四个业务源文件哈希与上轮一致，无页面、导航或数据存储代码改动。

## 未验证：需手机实测

- ADB 当前没有连接的 Android 设备，因此没有执行真机安装。
- 覆盖安装是否保留原有心情、夸夸、收支与照片；不要用卸载旧 App 的方式测试。
- 厂商启动器下猫狗图标的真实大小和遮罩。
- 系统结束 App 进程后记录能否从真实磁盘读取；本轮仅验证模拟存储与 Widget 重新加载。
- 实际相册/麦克风权限、取消选图、语音输入与系统键盘遮挡。
- 手机跨午夜及后台恢复后的日期与夸夸归属；本轮既有跨天测试仅验证指定日期的数据排列，不代表已验证真实时钟变化。

这些项目不能因为自动化测试通过就标记完成；不在缺少真机结果时继续重做界面。
