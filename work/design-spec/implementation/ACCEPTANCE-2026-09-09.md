# 2026-09-09 首页 v3 实施与验收记录

## 实施范围

- 首页标题改为当天中文日期与星期。
- 首页照片使用 `BoxFit.contain` 完整展示，最大高度 350 dp，并支持点开原图。
- 使用已锁定猫狗抠图，搭在相纸顶部留白，不重绘素材。
- 删除首页心情统计和已授权删除的低价值空状态文案。
- 新增“记下”和“夸夸墙”两个 46 dp 轻描边入口；前者直接打开新增编辑器，后者打开现有夸夸墙。
- 版本从 `0.8.0+8` 提升为 `0.9.0+9`。

未修改收支、回想、记下页面的信息结构、数据模型和保存格式；没有升级依赖。

## 自动验证

| 检查 | 结果 |
| --- | --- |
| 12 项批准文件 SHA-256 | 全部通过 |
| App 内今天/夸夸墙/回想猫狗与批准素材一致 | 全部通过 |
| `flutter analyze --no-pub` | 无问题 |
| `flutter test --no-pub` | 22 项通过 |
| 五种照片比例 | 16:9、9:16、1:1、8:1、1:8 几何约束与完整源区域通过 |
| 双入口触摸高度 | 小屏 1.3 倍字体下均不小于 44 dp |
| 文字对比度 | 正文 14.05:1、绿色操作 6.46:1、次要文字 5.03:1，均达到 WCAG AA |
| Flutter 实际截图 | `today-v3-actual.png`，已人工查看按钮文字、图标、照片与猫狗关系 |
| `flutter build apk --debug --no-pub` | 成功 |
| APK 元数据 | 包名 `com.lumanman.app`，versionName `0.9.0`，versionCode `9` |
| APK 签名 | APK Signature Scheme v2 校验通过，1 个签名者 |

APK SHA-256：`28F377EA886BF9B6310B7D1FE2662EB30BCFC85DB487EBE0436AE563CE74564F`

微信 ZIP SHA-256：`0B6B61FF53737F991887BFF18D8A632B134415BC4E718D22F16C39F8FBE442F9`

## 已知但未扩大处理的问题

- 构建仍提示 `speech_to_text` 使用 Kotlin Gradle Plugin 的未来兼容警告。
- Android SDK 命令行工具仍提示只理解 SDK XML v3，但读取到 v4。
- 两项均未阻止本次 Debug APK 构建；本轮不升级依赖或构建工具。

## 仍需真机验证

- ADB 未发现连接设备，尚未覆盖安装到用户手机。
- 覆盖安装后旧记录、夸夸、收支和照片是否保留。
- 实际相册选择、取消选图、麦克风权限、语音输入和键盘遮挡。
- 不同比例真实照片在目标手机上的完整显示与点开缩放。
- 厂商启动器中的猫狗图标遮罩与大小。
