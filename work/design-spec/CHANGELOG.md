# 路慢慢设计过程记录

本文件只记录已执行的设计步骤，不代表用户已经批准实现。用户未明确说“确认”前，效果图不得写入 App。

## 2026-09-09：首页 v3 接手完成并发布 v0.9.0

- 接手原任务被上下文压缩中断后的半成品；完整阅读设计决策和实施锁定清单，没有重做其他页面。
- 用户此前要求“做给我”，本次再次确认“好，继续”。首页 v3 已实施：中文日期和星期、照片等比完整相框、锁定猫狗、直接新增的“记下”入口、现有夸夸墙入口；移除首页心情统计及已授权删除的低价值空状态文案。
- 新批准基准为 `approved/today-page-v3.png`，旧批准稿保留。Flutter 实际渲染为 `implementation/today-v3-actual.png`。
- 修复首页测试死等：不再异步预缓存测试文件图片，改用 Flutter 约束与 `BoxFit.contain` 对 16:9、9:16、1:1、8:1、1:8 五种比例做确定性几何校验；空夸夸墙断言改为核对真实存在的标题和新增按钮。
- `flutter analyze --no-pub` 无问题；`flutter test --no-pub` 共 22 项通过；`flutter build apk --debug --no-pub` 成功。APK 版本 `0.9.0 (9)`，v2 签名有效。
- 交付文件：`release/lumanman-v0.9.0.apk`、`release/lumanman-v0.9.0-for-wechat.zip`。ADB 未发现设备，未执行真机覆盖安装。

## 2026-09-08：下半页双轻入口 v3（最新候选，待确认）

- 用户认为 v2 太空，附轻描边“记下”按钮局部参考。完整保存到 `references/today-quick-entry-feedback-2026-09-08.png`。
- 新建 `mockups/today-calm-v3-2026-09-08.html` / `.png`，只在原 v2 句子下方加“记下 / 夸夸墙”两个 46 dp 高描边入口，不加标题、统计、提醒或任务。拟定快捷新增与列表入口的区别，但未连接 App 或声称交互完成。
- 沿用 v2 的照片、相框、原猫狗、日期、句子、适配 CSS 与底部导航；旧候选与 approved 均保留。当前仅设计预览，待用户确认后才允许实施。

## 2026-09-08：完整适配与安静首页 v2（待确认，晚于当天第一候选）

- 保存用户新反馈：相框可保留、照片不能截断且需适配不同尺寸；删除圈出的整个心情区，其他页面也不要低价值空状态引导。原局部截图备份到 `references/today-mood-rejected-2026-09-08.png`。
- 新建 `mockups/today-calm-v2-2026-09-08.html` 与 PNG。停止用不完整手机截图的人像来示范，改用项目原有道路参考照片，完整展示该文件；明确告知用户其人像需要原文件，未补画。
- 保留锁定猫狗原文件、日期、相框、换图入口和导航；删除心情统计、空状态文案及重复“记下”按钮，不引入新任务、指标或提问；正文从粗体改常规字重。
- 原生 CSS 等比缩放，不裁切、不拉伸，横竖比例驱动自然流布局；照片可点开原文件，HTML 支持本地选图验证但不保存/上传用户照片。
- 使用代码几何图测试 16:9、9:16、1:1、8:1、1:8，浏览器输出 `5 PASS`；已查看完整边界，证据 `mockups/today-photo-fit-check-2026-09-08.png`。这是 HTML 布局检查，不是 Flutter 或真机验收。
- 只读检索四类空状态文案并保存待实施范围。未修改 App、APK、图标、业务逻辑、真实记录或其他页面；旧候选及批准图保留，新稿待用户确认。

## 2026-09-08：手机反馈后的今天页局部优化候选（待确认）

- 用户指出猫狗突兀，希望大标题“今天”换成“9月8日 星期二”，删除“今天拍下的”，先看优化图片。
- 原始反馈截图完整备份为 `references/today-phone-feedback-2026-09-08.jpg`。新预览为 `mockups/today-refinement-2026-09-08.html` 与同名 PNG；这是静态设计稿，不是 Flutter 实际运行截图。
- 仅使用锁定原件 `approved/today-cat-dog-cutout.png`，SHA-256 `21E4307F3839CEDCB13961364CB522502D7CA675B82EBFB0269A164D908F064B`。原件未修改；排版中等比缩小到 166 px，靠在 316 px 宽相纸上边框的右侧，爪子落在相纸留白、不遮挡照片。
- 人像仅展示反馈截图中可见的区域，未补画被遮挡的额头；不是原始完整照片。保留换图入口、心情数据与底部“今天”导航，无新增引导或来源文案。
- 新比例与旧锁定大幅猫狗布局不同，仅为用户本次要求的候选。已确认稿不覆盖；本次未修改 App、安装包、图标及其他页面。

## 2026-09-08：完善自动化验收，不改已确认 UI

- 新增三项验收测试：31 日切到二月的平年与闰年边界；320×568、1.3 倍字体下长回忆完整滚动、猫狗位于文字之后；小屏夸夸新增与修改后重新挂载 App，验证 ID、创建时间、修改内容与历史记录保留。
- 修改范围仅 `app/test/recall_test.dart`、`app/test/widget_test.dart` 及本地记录。为隔离连续 Widget 测试的资源 Future 缓存，每项测试前清理测试环境的 rootBundle 缓存；不改生产代码。
- 19 项测试通过，Debug APK 构建成功。构建仍提示既有 speech_to_text Kotlin Gradle 与 SDK XML 兼容性警告，没有为此更新依赖或改配置。
- `app/lib/` 四个文件哈希与 2026-09-07 一致，页面设计、图标、存储逻辑和个人数据均未修改。
- ADB 未发现连接的 Android 设备，没有安装到手机；模拟存储测试不等同于真实磁盘、系统杀进程或覆盖安装验收。详细结果见 `implementation/ACCEPTANCE-2026-09-08.md`。

## 2026-09-07：已确认的猫狗图标替换到 Android

- 用户再次查看已锁定的猫狗图标后明确授权“可以，可以替换掉了”。已完整核对设计决策与实施锁定清单，全部十二项锁定文件哈希通过。
- 唯一源文件为 `approved/app-icon.png`，SHA-256 `C416E2BDBD5A9AE31735940A2348A3AA4529CEAAF1565DADA781078048FD6A61`。没有生成近似图，没有改变宠物或构图。
- 替换 Android mdpi/hdpi/xhdpi/xxhdpi/xxxhdpi 的 `ic_launcher.png` 与 `ic_launcher_foreground.png` 共十张；`drawable/ic_launcher_background.xml` 背景改为暖陶土色 `#BB4C2C`，保留现有 Manifest 与 adaptive-icon 引用。
- 生成及尺寸检查脚本：`implementation/build-android-icons.ps1`；基于实际生成资源的圆形、圆角方形及 48 px 遮罩模拟预览：`implementation/app-icon-android-preview.png`。已查看确认两只宠物的脸可辨认。
- `flutter analyze --no-pub` 无问题；`flutter test --no-pub` 共 16 项通过；`flutter build apk --debug --no-pub` 成功。包内十张 PNG 与源资源逐像素一致，包内 XML 背景色为 `0xffbb4c2c`。
- 安装包：`app/build/app/outputs/flutter-apk/app-debug.apk`。构建仍有 speech_to_text 的 Kotlin Gradle 兼容性警告，本次未改动该依赖；未执行手机安装或声称真机验收。
- `app/lib/` 四个源文件前后哈希一致；所有页面、原记录、导航保持不变。图标替换已完成，本地记录已更新，下一步可验收手机实际显示，不继续扩改页面。

## 2026-09-07：回想页按确认稿实施

- 按用户“继续”授权完成回想页，不修改今天、夸夸墙、收支、记下或 App 图标。
- 日历改为紧凑排布，月份居中、年份同步切换，照片日期使用橙色选中边框。详情按所选日期展示心情与夸夸，使用色点和文字标签；保留有账目日期的收支摘要。
- 使用锁定的 `approved/recall-cat-dog.png` 原文件，复制到 `app/assets/stickers/recall-cat-dog-exact.png`，两文件 SHA-256 同为 `B1EE4C5F2952D05D4CBE39AA18910A178920EE36D64F4EBFC1DBBF476F23E4D7`。
- 首次渲染发现猫狗在首屏被底部导航截断，已收紧日历与详情间距；360×800 参考记录画面中的猫狗可完整显示在详情下方，无文字遮挡。
- 实际 Flutter 渲染保存为 `implementation/recall-page-actual.png`。使用原道路照片和紫色合照，不使用生成替代图；三条文字取自已核对的参考记录。渲染测试在隔离存储中执行，未将参考数据写入用户个人存储。
- 新增 `app/test/recall_test.dart`，验证日期切换、记录隔离、空日期与跨年月份显示；`flutter analyze --no-pub` 无问题，`flutter test --no-pub` 共 16 项通过。
- `flutter build apk --debug --no-pub` 成功，安装包位于 `app/build/app/outputs/flutter-apk/app-debug.apk`。构建提示 speech_to_text 的 Kotlin Gradle 兼容性与 SDK XML 版本警告，本次未改动这些依赖；尚未在真实手机安装测试。
- 当前回想页已实施、实际画面待用户验收。下一项仍为已确认的 App 图标，未在本次同时实施。

## 2026-09-04：收支页按确认稿实施并改为轻量宠物元素

- 用户明确开始实施收支页，并在实际渲染检查时取消页面下方整只猫狗，允许重新设计为不遮挡账目的猫狗元素。
- 保留确认稿的票据式月度摘要、收入/支出切换、无边框流水与绿色记账按钮；所有金额继续读取真实本机数据，不硬编码效果图数字。
- 摘要票据右上角使用淡爪印压章，流水末尾使用三枚低对比度脚印；没有加入引导文案。
- 保留原有新增、编辑、删除与本机存储逻辑；“今天”“夸夸墙”“回想”“记下”和 App 图标未修改。
- 原锁定猫狗素材继续保存在 `approved/ledger-cat-dog.png`，没有删除或替换；当前收支页不调用整只猫狗。
- 实际渲染截图保存为 `implementation/ledger-page-actual.png`。

## 2026-09-04：收支页实施前再次核对并暂停

- 已重新展示并核对锁定设计稿 `approved/ledger-page.png`，文件 SHA-256 与锁定清单一致。
- 已核对锁定猫狗素材 `approved/ledger-cat-dog.png`，文件 SHA-256 与锁定清单一致。
- 本步骤只完成实施前确认与本地记录，没有修改收支页或其他 App 页面。
- 当前停在“等待用户明确开始实施收支页”的节点。

## 2026-09-04：夸夸墙跨天规则确认并实施

- 用户确认所有夸夸都要保存；只有每天第一条在当天使用顶部猫狗特殊纸条。
- 当天后续夸夸全部保留为普通便签；新一天第一条进入特殊纸条后，旧日期全部记录向下排列。
- 历史日期从近到远，同一天保持原记录先后顺序；顶部次数只统计今天。
- 没有增加任何引导、提问、副标题或解释文字。
- 新增跨天排列回归测试，覆盖当天第一条、当天后续记录与历史记录不丢失。
- `implementation/praise-wall-actual.png` 已更新为跨天多条记录的实际渲染。

## 2026-09-04：夸夸墙跨天规则记录（未实施）

- 用户指出夸夸墙尚未修改完成，并询问顶部猫狗纸条是否代表每天的第一条夸夸。
- 已如实记录当前实现：顶部为今天最新一条、第二张为今天次新一条；第二天不会把前一天内容自动下移到夸夸墙历史列表。
- 已记录目标方向：每天第一条占据顶部猫狗纸条，次日由新一天第一条替换，旧的每日第一条依次向下排列。
- “同一天第二条及后续夸夸如何呈现”仍待用户确认；本次只保存规则，没有继续修改 App。

## 2026-09-04：夸夸墙按确认稿实施

- 仅修改“夸夸墙”：标题完整显示，右侧保留今日夸夸次数。
- 使用锁定的猫狗素材共同递出最新一张纸条；第二张使用独立浅绿色便签，不重复宠物头像。
- 删除复杂引导文案，只保留夸夸正文、时间和底部“贴一张夸夸”入口。
- 保留原有新增、编辑、长按删除和本机存储逻辑；“记下”和其他页面未修改。
- 实际渲染截图保存为 `implementation/praise-wall-actual.png`。

## 2026-09-01：今天页按确认稿实施

- 已逐项校验 `approved/` 中全部锁定文件的 SHA-256，与 `IMPLEMENTATION-LOCK.md` 一致。
- 仅修改“今天”页：使用锁定的白狗 + 狸花猫抠图，自然衔接可替换照片上边线；未改变宠物动作、身体、比例、爪子或神态。
- 名言区域只保留正文；心情摘要改为并排的文字与色点信息，不重复使用宠物头像。
- 保留原有照片选择、心情跳转、收支与夸夸墙数据逻辑；“记下”和其他页面未修改。
- 实际渲染截图保存为 `implementation/today-page-actual.png`。

## 2026-09-01：全部已确认设计与图标本地锁定

- 用户确认 App 图标候选稿 v1。
- 已将“今天”“夸夸墙”“收支”“回想”“记下”保护基准、App 图标及各页猫狗素材复制到 `work/design-spec/approved/`。
- 新增 `IMPLEMENTATION-LOCK.md`，记录全部锁定文件、SHA-256、不可遗漏要求与实施校验顺序。
- 当前只完成本地归档，没有修改 App。

## 2026-09-01：App 图标候选稿 v1

- 猫与狗作为同等视觉主体共同出现，使用暖陶土色底，避开用户否定的白绿单狗方案。
- 图标不加入文字、爪印、爱心、场景和其他装饰，保持 Android 小尺寸辨识度。
- 使用内置图像生成能力生成主体，并制作 84 px、48 px 缩小预览。
- 仅保存候选稿与预览，尚未替换 App 正式图标。

## 2026-09-01：回想页确认，进入 App 图标设计

- 用户明确确认“回想”页面。
- `mockups/recall-v1.png` 已锁定为实施基准。
- “记下”页面继续按已确认保护基准保留。
- 下一步只设计猫狗共同出现的 App 图标稿，确认前不替换正式图标。

## 2026-09-01：回想页面效果图 v1

- 8月28日、29日日历缩略图使用用户原来的道路照片与紫色合照，没有用生成图片替换。
- 8月28日详情只采用用户截图中可核对的三条内容：开心、不开心与夸夸。
- 猫狗改为共同整理照片的完整身体动作，不作为日历头像或重复贴纸。
- 仅生成单页效果图与本地记录，未修改 App。

## 2026-09-01：收支页确认，进入回想页设计

- 用户明确确认“收支”页面。
- `mockups/ledger-v1.png` 已锁定为实施基准。
- 下一步只设计“回想”单页效果图，仍不修改 App。

## 2026-09-01：收支页面效果图 v1

- 使用用户现有收支截图中可核对的金额和两笔工资记录，没有虚构财务数据。
- 本月结余以单张票据式摘要呈现，收入与支出切换保持清楚。
- 流水改为安静的无边框列表，只保留必要分隔线。
- 新增专用于收支页的猫狗整理空白票据场景，不重复使用头像贴纸。
- 仅生成单页效果图与本地记录，未修改 App。

## 2026-09-01：夸夸墙确认，下一步为收支页

- 用户明确确认“夸夸墙”页面。
- `mockups/praise-wall-v1.png`（标题完整显示修正版）已锁定为实施基准。
- 下一步只设计“收支”单页效果图，仍不修改 App。

## 2026-09-01：夸夸墙标题显示修正 v1.1

- 修复猫狗素材底色层遮挡“夸夸墙”标题下半部分的问题。
- 只调整标题图层顺序，其他布局、文字、猫狗和纸条均未修改。
- 修正规则已写入本地设计决策，未修改 App。

## 2026-09-01：今天页确认，进入夸夸墙单页设计

- 用户明确确认“今天”页面。
- “今天”页面效果图 `mockups/today-exact-cat-dog-v1.png` 已锁定为实施基准。
- 下一步仅设计“夸夸墙”，仍不修改 App。

## 2026-09-01：夸夸墙效果图 v1

- 保留“夸夸墙”和今日次数，只展示真实夸夸内容与时间。
- 未恢复用户要求删除的两组引导文案。
- 新增专用于本页的猫狗陪伴角色，以完整身体动作共同递出空白纸条，不使用重复头像贴纸。
- 第一条夸夸直接写在猫狗递出的纸条上；第二条使用独立浅绿色便签。
- 仅生成单页效果图与素材，未修改 App。

## 2026-09-01：今天页名言说明文字精简 v1.1

- 按用户要求删除名言下方的“用户提供 · 来源待核验”。
- 固化规则：首页名言不展示来源核验、用户提供、示例排版等内部说明文字，只保留句子正文。
- 仅更新单页效果图与本地设计记录，未修改 App。

## 2026-09-01：今天页猫狗与照片衔接稿 v1

### 用户要求

- 使用用户指定的白狗 + 狸花猫原图。
- 猫狗保持原动作、身体、爪子、比例和神态。
- 照片从猫狗身下的基准线开始，前肢自然压进照片区域。
- 配合已经确认的五页 UI 方向制作单页效果图。
- 先给用户看，不修改 App。

### 已执行

1. 校验锁定原图 `references/cat-dog-photo-edge-exact.png`，SHA-256 为 `CF9B010005554A6537CCA61FC637CA28DD7B69B731FD07F0B6EA31CBA0E492A4`。
2. 只对锁定原图进行棋盘背景抠除与边缘清理；没有使用生成式图片替换宠物。
3. 从原始“今天”页面截图中提取用户当天的道路照片。
4. 按 `references/five-page-approved-direction.png` 的暖纸张、深墨绿、低饱和橙色和信息层级制作 360 × 800 单页效果图。
5. 猫狗主体放在照片上方，照片从它们身体下方开始，原有三只可见前爪自然越过照片顶边。
6. 心情摘要未重复使用猫狗头像，避免贴纸堆叠。

### 产物

- 效果图：`mockups/today-exact-cat-dog-v1.png`
- 对话预览：`mockups/today-exact-cat-dog-v1.html`
- 原图抠图：`mockups/cat-dog-photo-edge-exact-cutout.png`
- 渲染脚本：`mockups/render_today_exact.py`

### 当前状态

- 等待用户确认或提出局部修改。
- 尚未修改 `app/` 下的任何 Flutter 页面、资源配置或业务代码。
