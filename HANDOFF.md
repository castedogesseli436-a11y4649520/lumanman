# 路慢慢接手状态（2026-09-10）

## 产品与源码地图

本机生活记录 App。暖米白、深墨绿、少量橙色、猫狗陪伴视觉；运行时读取用户实际数据。没有后台或云端数据库。

| 文件 | 责任 |
| --- | --- |
| app/lib/main.dart | App 入口、主题、底部导航、心情列表、编辑弹层、照片入口、语音转文字 |
| app/lib/journal.dart | 心情记录模型、照片复制/清理、本地读写、语音文本合并 |
| app/lib/life_data.dart | 收支、夸夸、每日封面、每日句子的数据与存储 |
| app/lib/life_pages.dart | 今天、收支、回想及夸夸墙 UI |
| app/test/ | 现有单元/Widget 测试，不能代替真机权限与语音验收 |
| app/android/ | Android 构建、包名、权限、图标、原生入口 |
| app/assets/ | 正式运行的猫狗图片和句子词库 |

已有：开心/不开心记录、后续思考、照片、收入/支出、夸夸墙、按日期回想、今日照片、中文日期、每日句子、系统语音转文字入口。主页已实施 v3，以 approved/today-page-v3.png 为最新基准；旧文件和历史“待确认”文字是过程记录，按最新日期和源码判断。

## 本机存储，不等于同步或备份

SharedPreferences 中保存 JSON：journal_v1（心情）、money_v1（账目）、praise_v1（夸夸）、daily_covers_v1（每日封面）。照片经 path_provider 存在应用文档目录的 journal_photos/ 与 cover_photos/。记录包含本机文件路径。

源码包中的参考照片/测试样例不是用户手机数据。当前没有账号、云同步、导出恢复界面或社区后端。换电脑开发无需搬手机数据；换手机使用则必须另外设计导出与恢复，覆盖记录和照片并重建路径，不能只复制 JSON。不要清空 SharedPreferences 或卸载旧应用来排查语音故障。

## 语音问题：仍待定位和真机验收

用户确认需求为普通话语音转文字；此前反馈“权限都开了，但是没反应”，未提供品牌型号/系统版本，也未反馈 0.9.1 的实际结果。未连接其手机获取日志。

0.9.1 实际代码变化：启动时加状态提示、指定 zh_CN、异常捕获从 PlatformException 扩展到 Exception、增加初始化状态标志。初始化仍发生在点击麦克风时，未移到页面启动时；先前聊天中的计划与实际实现有差异，以代码为准。

已确认插件 listen 会把部分 PlatformException 转成 ListenFailedException，旧版捕获范围有遗漏；这只证明错误处理缺陷，不能证明用户手机的根因就是该异常，也不能保证转写已恢复。历史“已修复没反应”的结论应按此边界理解。

交接后的优先检查：

1. 用目标手机复现，记录型号、Android 版本、0.9.1 页面状态及实际异常；检查系统 RecognitionService 是否可用。
2. 检查 SpeechToText 单例/回调生命周期，反复打开编辑弹层后回调是否仍指向已关闭页面。
3. 核对启动成功、初始化延迟、关闭弹层时异步返回、停止/错误事件的状态次序；当前直接将 _listening 设 true 不等于底层已经开始采集。
4. 确认系统支持 zh_CN，网络识别服务可访问；权限允许不能保证识别服务可用。
5. `debugLogging: true` 当前开启。排障后审查并关闭不必要的详细日志，不分享含私人文本的日志。

现有测试只覆盖语音文字合并与入口显示，没有真实识别，也没有完整语音异常/生命周期自动测试。若需要改为独立云端识别，需先确定服务、费用、声音上传/隐私规则和服务端鉴权，不能把服务密钥写进 App。

## 验证事实

历史工具输出：0.9.1 修改过程中 flutter analyze 通过、22 项测试通过；随后调整结束状态文字并格式化，Debug APK 构建成功。没有真机识别、真机覆盖安装、iOS 编译结果。迁移副本的检查结果另见 TRANSFER-VERIFICATION.md。

已见构建警告：speech_to_text 的 Kotlin Gradle Plugin 未来兼容性、SDK XML 解析版本提示。未据此升级依赖，也未证明其为语音根因。

## Android 签名

现有 APK 使用 Android Debug 证书，release 配置也暂时指向 debug 签名，尚不是正式发行签名体系。独立密钥包中的 debug.keystore 已与 0.9.1 APK 核对证书 SHA-256 一致：

`DAD2611C859001D96593FE900E1F74C7E58EACEC681089C09DC845807D05CA40`

覆盖安装需保持包名、兼容的版本号和签名。每次发新版本增加 pubspec.yaml 的版本和 build number。不要以替换全局 .android/debug.keystore 的方式影响新电脑的其他项目；使用独立密钥包内的项目级配置说明。

## iOS 与社区方向

- 用户希望 iPhone 可安装，当前没有 ios/ 工程、签名材料或 IPA；此前仅讨论借用/云端 Mac 打包方案，没有执行。
- 用户提出双向数据互通、点赞和评论，意图是社区；当前没有任何已实现社区功能。
- 助手建议保留私人日记、主动选择公开、再加账号/个人同步/帖子/点赞/评论/举报与管理。这是建议范围，尚未确认发布地区、公开注册还是熟人测试、预算、登录方式及后端供应商。
- 下一位开发者需先确认社区范围及默认隐私，再实施。私人记录不得因添加社区而自动公开。不要把讨论中的方案冒充已批准详细需求。

## 迁移副本与原工程的差异

Flutter 业务源码、测试与资源保持原样。仅迁移副本将 Gradle 本地 file 地址改为官方 HTTPS 下载地址，并增加根交接文档、Git 忽略规则、交付校验清单和校验脚本。原工作目录未被替换。本交接文档记载聊天中已知事实，不包含完整聊天记录。
